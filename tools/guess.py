#!/usr/bin/python
import re

#f = open("ctp.log")
#log = f.read()
#lux = re.findall("lux_clear=(\d+), lux_ir=(\d+)", log)
#print "\n".join(ch0 + " " + ch1 + " " + str(float(int(ch1))/int(ch0)) for ch0, ch1 in lux)

lux = [(100, 52), (150, 85), (200, 142), (250, 187), (300, 232), (400, 355), (500, 430), 

